[project]
name = "vibesource-browser-use-scraper"
version = "0.1.0"
description = "Minimal Python scraper runner for Vibesource"
requires-python = ">=3.12"
dependencies = [
  "browser-use>=0.2.0",
  "playwright>=1.54.0",
]

[tool.uv]
package = false
